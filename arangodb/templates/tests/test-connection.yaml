apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arangodb.fullname" . }}-test-connection"
  labels:
    {{- include "arangodb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arangodb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
